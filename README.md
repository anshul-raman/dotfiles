# dotfiles

## Create symlinks 
  - ./nvim -> ~/.config/nvim  
  - ./.tmux.conf -> ~/.tmux.conf


### TMUX setup 

  - Clone TPM 

> ```bash
> $ git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
> ```

  - Install Plugins 
  > `prefix` + `I` 

